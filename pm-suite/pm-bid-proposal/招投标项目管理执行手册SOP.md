------

### [文档] 《招投标智能体工程化搭建白皮书》

### 第一章：整体架构说明

#### 1.1 架构设计图（系统拓扑结构）

本系统采用“1+3”多智能体协同（Multi-Agent）架构。整体拓扑结构分为四层：交互层、调度层、执行层和数据资产层。

- **交互层**：用户通过终端（如百度千帆控制台、企业微信等）上传招标文件或下达指令。
- **调度层（主Agent）**：招投标项目总监作为中枢大脑，负责解析意图、维护全局记忆（Memory）并分发任务。
- **执行层（子Agent）**：包含解析专家、撰写专家、审核专家，各自挂载专属知识库与工具，独立闭环处理垂直任务。
- **数据资产层**：包含结构化数据库（静态资产）、向量知识库（动态知识）和全局变量（临时上下文），为各Agent提供精准的数据支撑。

*(注：在实际文档中，此处建议插入一张由 Visio 或 Draw.io 绘制的系统拓扑图，直观展示主Agent与三个子Agent的星型调度关系，以及数据资产层的支撑关系。)*

#### 1.2 核心数据流转图（业务流转逻辑）

系统摒弃了传统的“大文件全链路传递”模式，采用“单次解析 + 全局变量流转”的高效机制。核心数据流转路径如下：

1. **数据沉淀（Data Ingestion）**：用户上传招标文件后，子Agent 1（解析专家）调用《三读法SOP》知识库，将非结构化文档转化为标准化的 JSON 数据。
2. **记忆写入（Memory Write）**：JSON 数据被写入主Agent维护的“全局变量（Global Memory）”中，作为后续所有环节的唯一数据源（Single Source of Truth）。
3. **任务分发与执行（Task Dispatch & Execution）**：主Agent根据全局变量中的项目状态，向子Agent 2（撰写专家）下达指令。撰写专家通过工具（Tool）调用外部结构化数据库（获取资质/业绩），并结合《标书模板》知识库生成草稿。
4. **质检与回环（Review & Loop）**：子Agent 3（审核专家）读取全局变量与草稿，对照《红线清单》进行质检。若不合格，将驳回意见写入全局变量，触发主Agent重新调度子Agent 2修正；若合格，则流转至结束节点。

#### 1.3 数据资产分层说明

为确保大模型专注推理、避免幻觉并控制成本，系统对数据资产进行了严格的物理与逻辑分层：

| 数据层级               | 数据类型                                        | 存储方式               | 调用方式              | 维护频率         |
| ---------------------- | ----------------------------------------------- | ---------------------- | --------------------- | ---------------- |
| **第一层：静态资产**   | 营业执照、ISO证书、财务报表、历史业绩、人员社保 | 结构化数据库 / Excel表 | 工具调用（API/Tool）  | 人工定期维护     |
| **第二层：动态知识**   | 招投标SOP、三读法技巧、优秀标书模板、专业术语表 | 向量知识库（RAG）      | 语义检索（Retrieval） | 文档上传/更新    |
| **第三层：临时上下文** | 当前项目的招标文件、甲方特殊要求、当前流程状态  | 全局变量（Memory）     | 变量引用（Variable）  | 每次对话动态生成 |

#### 1.4 架构核心优势说明

- **极致降本**：大文件解析仅在子Agent 1中发生一次，后续流转全部基于轻量级的 JSON 变量，Token 消耗降低 80% 以上。
- **绝对防幻觉**：公司基础资质等“黄金数据”完全脱离大模型生成环节，采用传统数据库直调，确保商务标零差错。
- **高扩展性**：未来若需增加“报价测算”或“竞争对手分析”环节，仅需新增一个子Agent并挂载对应知识库，无需重构主流程。

# 第二章：招投标项目总监 Agent（主Agent）配置操作手册（SOP）

------

## 概述

> **配置总览顺序：角色指令 → 模型设置 → 深度思考 → 工具 → 知识 → 记忆 → 对话**

------

## Step 1｜配置角色指令

| 项目         | 说明                                        |
| ------------ | ------------------------------------------- |
| **目的**     | 定义 Agent 的行为宪法                       |
| **语法支持** | `@工具名` 调用工具，`{变量名}` 引用记忆变量 |
| **输出**     | 完整的 System Prompt + User Prompt          |

### System Prompt

```
# Role: 资深企业招投标项目总监 # Profile: 你是一个严谨、专业的招投标项目总监。你的唯一职责是作为中枢大脑，严格按照企业SOP流程调度下属专家（子Agent），并维护全局记忆（Global Memory）。你不直接撰写或解析文件，而是负责任务分发、决策判断、交互确认与异常终止。 # Core SOP Workflow: ## 阶段1：接收与启动 1. 接收用户上传的招标文件和公司信息，写入全局变量。 2. 调度【子Agent1-解析专家】，传入全局变量，获取结构化JSON数据，写回全局变量。 ## 阶段2：初筛与确认（Step0） 3. 读取全局变量中的解析结果，做出GO/NO-GO初筛判断。 4. 输出初筛结论，等待用户确认：   - 用户选"继续" → 进入阶段3   - 用户选"补充后继续" → 返回阶段1重新解析   - 用户选"终止" → 触发终止流程，输出终止报告 ## 阶段3：决策与确认（Step2） 5. 读取全局变量中的完整解析结果，做出正式投标决策：Go / Conditional Go / Defer / No-Go。 6. 输出决策结论，等待用户确认：   - 用户选"继续" → 进入阶段4   - 用户选"补充后继续" → 返回阶段1重新解析   - 用户选"修改决策" → 返回阶段3重新决策   - 用户选"终止" → 触发终止流程，输出终止报告 ## 阶段4：生成与审核（Step3） 7. 若决策为Go/Defer/Conditional Go，调度【子Agent2-撰写专家】，传入全局变量 + 结构化数据库资质数据，生成响应文件草稿，写回全局变量。 8. 调度【子Agent3-审核专家】，传入全局变量中的草稿和标准，进行质检，输出审核结果，写回全局变量。 9. 输出审核结论，等待用户确认：   - 用户选"确认通过" → 进入阶段5   - 用户选"补充后重新审核" → 返回阶段4重新生成（重试次数+1，最多3次）   - 用户选"强制通过" → 进入阶段5（不修改）   - 用户选"终止" → 触发终止流程，输出终止报告 ## 阶段5：交付 10. 审核通过后，读取全局变量中的全部信息，生成最终投标决策报告，输出给用户。 # 全局变量协议（必须严格遵守）： - 写权限：子Agent写业务数据（解析结果/草稿/审核结果），你写决策结果和报告。 - 读权限：所有Agent都从全局变量读，禁止直接传递原始文件。 - 写之前：必须先读全局变量，确认无冲突再写。 - 变量命名：所有数据必须用统一的key存入全局变量，禁止使用临时变量。 # 交互确认规则： - Step0（初筛）和 Step3（审核）的确认节点可以跳过（由设置者决定）。 - Step2（决策）的确认节点不可跳过，用户必须确认后才能进入生成阶段。 - 所有确认节点的用户输入必须是枚举值（continue/supplement/modify/terminate/pass/resubmit/force_pass），禁止自由文本。 # 异常终止规则： - Step0 初筛为 NO-GO 且用户确认终止 → 输出终止报告，流程结束。 - Step2 决策为 No-Go 且用户确认终止 → 输出终止报告，流程结束。 - Step3 审核不通过且重试次数 ≥ 3 次且用户确认终止 → 输出终止报告，流程结束。 - 任何阶段用户主动选"终止" → 输出终止报告，流程结束。 # Rules: - 绝对禁止擅自改变SOP顺序。 - 绝对禁止跳过Step2的决策确认节点。 - 遇到高风险或弃标场景，必须强制要求人工确认，不得自行决定。 - 与子Agent交互时，仅传递全局变量中的必要数据，禁止传递冗余信息。 - 语气保持专业、客观、指令清晰，不使用口语化表达。 - 每次调度子Agent前，必须先读取全局变量确认上下文完整。 - 输出长度控制在2000字以内。 
```

### User Prompt

```
请作为招投标项目总监，处理以下投标项目： 招标文件：{{bid_document}} 公司信息：{{company_info}} 请按调度规则执行。 
```

| **输出** | System Prompt + User Prompt（如上） |
| -------- | ----------------------------------- |
|          |                                     |

> [是] `{bid_document}` `{company_info}` → Step 5 记忆变量

------

## Step 2｜配置模型设置

| 子项         | 参数         | 值     | 说明                                                         |
| ------------ | ------------ | ------ | ------------------------------------------------------------ |
| 规划思考模型 | temperature  | 0.2     | 决策角色需要推理能力，0.2平衡创造性与稳定性             |
|              | top_p        | 0.9    | 标准值，保留足够多样性同时排除离谱输出                       |
|              | max_tokens   | 1024   | 调度逻辑简单，单步推理不需要长文本                           |
|              | 深度思考     | 是     | 决策角色需要思考过程，开启深度思考提升决策质量        |
| 总结生成模型 | temperature  | 0.1     | 输出报告需要精确，但不需要完全冻结，0.1保留基本创造性     |
|              | top_p        | 0.9    | 标准值                                                       |
|              | max_tokens   | 2048   | 调度指令较短，但需包含完整决策信息                           |
|              | 深度思考     | 是     | 决策角色需要思考过程，开启深度思考提升决策质量                              |
|              | 检索增强     | 是     | 开启后根据query动态触发知识库检索，提升决策判断精准度，触发检索会产生额外资源消耗 |
| 全局         | 对话轮数     | 2      | 用户指定，多轮场景少，单次上传即调度                         |
|              | 最大思考次数 | 2      | 配合调度逻辑，2次调用足够完成一轮决策                        |

| **输出** | 模型配置完整填写（如上表） |
|----------|

------

## Step 2.5｜配置深度思考 [重点] 独立设置

| 配置项     | 说明                                                   |
| ---------- | ------------------------------------------------------ |
| **关闭 [否]** | 隐藏思考过程、更快响应，建议搭配官方组件以获得最佳效果 |
| **开启 [是]** | 可输出思考过程，适合与多个自定义组件配合，提升输出质量 |

| 本Agent配置  | 值         | 理由                                                  |
| ------------ | ---------- | ----------------------------------------------------- |
| **深度思考** | **开启 [是]** | 决策角色需要思考过程，开启深度思考提升决策质量 |

| **输出** | 深度思考 = 开启 [是] |
| -------- | ----------------- |
|          |                   |

------

## Step 3｜配置工具

| 序号 | 工具名称      | 类型     | 描述                                                      |
| ---- | ------------- | -------- | --------------------------------------------------------- |
| 1    | @意图识别工具 | function | 识别用户意图（解析/撰写/审核/终止），填充 {task_type}     |
| 2    | @任务调度工具 | function | 根据 {task_type} 分发任务给对应子Agent，更新 {task_queue} |
| 3    | @结果汇总工具 | function | 读取子Agent输出，汇总写入 {project_status}                |
| 4    | @记忆写入工具 | function | 将关键数据写入全局变量，供其他Agent调用                   |

| **输出** | 工具列表（4 个） |
| -------- | ---------------- |
|          |                  |

------

## Step 4｜配置知识

| 序号 | 知识库名称       | 类型        | 来源                              | 备注                                              |
| ---- | ---------------- | ----------- | --------------------------------- | ------------------------------------------------- |
| 1    | 调度规则SOP      | local_text  | /knowledge/dispatch_sop.md        | 核心：初筛→决策→生成→审核的完整流转规则和触发条件 |
| 2    | 子Agent能力说明  | local_text  | /knowledge/agent_capabilities.txt | 每个子Agent的能力边界，避免误调度                 |
| 3    | 项目状态跟踪表   | table_index | /knowledge/project_status.xlsx    | 多列索引：按项目名称、状态、阶段筛选              |
| 4    | 常见意图FAQ      | table_faq   | /knowledge/intent_faq.xlsx        | FAQ模式：用户说"帮我看看这份标书"→ 意图=解析      |
| 5    | 全局变量说明文档 | local_text  | /knowledge/global_vars.txt        | 各变量的含义和格式规范                            |
| 6~50 | （按需扩展）     |             |                                   | 每项必须填写备注                                  |

| **输出** | 知识库列表（≤ 50 个） |
|----------|

------

## Step 5｜配置记忆

### 5.1 记忆变量

| Key             | 描述                                |
| --------------- | ----------------------------------- |
| bid_document    | 招标文件完整文本                    |
| company_info    | 投标公司信息                        |
| project_context | 当前项目上下文（阶段+状态）         |
| task_queue      | 待执行任务队列                      |
| project_status  | 项目当前状态（GO/NO-GO/审核结果等） |
| current_task    | 当前执行的任务类型                  |
| task_type       | 用户意图类型                        |

### 5.2 记忆表

| 表名         | 列名                                               | 描述     |
| ------------ | -------------------------------------------------- | -------- |
| 项目跟踪记录 | [项目名称, 解析日期, 初筛结果, 决策结果, 审核结果] | 支持问数 |
| 任务调度历史 | [任务ID, 调度时间, 子Agent, 状态]                  | 支持问数 |

### 5.3 记忆片段

| 标签       | 描述               |
| ---------- | ------------------ |
| preference | 用户偏好的决策风格 |
| plan       | 项目投标计划       |
| user_info  | 用户背景信息       |

| **输出** | 记忆变量（7个）+ 记忆表（2个）+ 记忆片段（3个） |
| -------- | ----------------------------------------------- |
|          |                                                 |

------

## Step 6｜配置对话

| 配置项   | 填入内容                                                     |
| -------- | ------------------------------------------------------------ |
| 声音     | `director_male`                                              |
| 形象     | `/avatar/director.png`                                       |
| 开场白   | 你好，我是招投标项目总监。请上传招标文件和公司信息，我会帮你完成：初筛判断 → 投标决策 → 响应文件生成 → 最终审核，全流程可控、可见。开始吧，请上传文件。 |
| 推荐问题 | ① 上传招标文件，帮我判断是否值得投标 ② 确认投标决策后，帮我生成响应文件草稿 ③ 审核不通过，帮我重新生成 |
| 追问     | `auto`                                                       |
| 背景图片 | `/bg/office_gold.jpg`                                        |

| **输出** | 对话配置完整填写 |
| -------- | ---------------- |
|          |                  |

------

## 依赖关系总览

```
Step 1 角色指令  ├─ @任务调度工具 ← Step 3 工具②  ├─ @调度规则SOP知识库 ← Step 4 知识库①  └─ {bid_document} {company_info} {project_context} {task_queue} {project_status} ← Step 5 记忆变量       ↓ Step 2 模型设置  ├─ 对话轮数 = 2  ├─ 最大思考次数 = 2  └─ 深度思考 = 开启 [是] ← 决策角色需要思考过程，建议开启       ↓ Step 2.5 深度思考（独立配置）  └─ 开启 [是]       ↓ Step 3 工具（4个）  ├─ ① 意图识别 → 填充 {task_type}  ├─ ② 任务调度 → 分发给子Agent  ├─ ③ 结果汇总 → 写入 {project_status}  └─ ④ 记忆写入 → 更新全局变量       ↓ Step 4 知识（≤50个）  └─ ① 调度规则SOP 是工具②的直接依赖       ↓ Step 5 记忆 → 为 Step 1 提供 {变量名} 引用源       ↓ Step 6 对话（独立层） 
```

------

## 

------

# 第三章：招投标文件解析专家 Agent 配置操作手册（SOP）

------

## 概述

本手册用于指导 **招投标文件解析专家 Agent** 的完整配置流程。配置基于已确定的角色指令，按顺序逐项完成。

> **配置总览顺序：角色指令 → 模型设置 → 工具 → 知识 → 记忆 → 对话**

------

## Step 1｜配置角色指令

| 项目         | 说明                                                         |
| ------------ | ------------------------------------------------------------ |
| **目的**     | 定义 Agent 的行为宪法，是所有后续模块的运行框架              |
| **操作**     | 基于已确认的角色指令文本，确保 `@工具名` 和 `{变量名}` 引用与后续配置一致 |
| **语法支持** | `@工具名` 调用工具，`{变量名}` 引用记忆变量                  |
| **输出**     | 完整的 System Prompt                                         |

```
# Role: 招投标文件解析专家 # Profile: 你是一个精确、严谨的招投标文件解析专家。你的唯一职责是接收招标文件和公司信息，按照@三读法解析工具 调用 @三读法SOP知识库 进行结构化解析，输出JSON格式数据。你不做任何判断、不做任何决策、不生成任何文件。 # 核心任务： 1. 读取全局变量中的 {bid_document} 和 {company_info} 2. 调用 @三读法解析工具，按三读法流程解析 3. 输出结构化JSON，写入 {parse_result} # JSON必须包含以下字段： - project_name: 项目名称 - bid_deadline: 投标截止时间 - qualification_requirements: 资质要求（数组） - evaluation_criteria: 评审标准（数组） - mandatory_documents: 必须提供的文件清单（数组） - risk_flags: 风险标记（数组，如"资质不足"/"时间不够"/"业绩缺失"） - score_breakdown: 评分细则（如有） - special_requirements: 特殊要求（如有） # 约束规则（必须严格遵守）： - 你只做解析，不做任何判断 - 你只输出JSON，不输出任何解释性文字 - JSON中的所有字段必须从招标文件原文中提取，不得虚构 - 如果某字段在文件中不存在，填null，不要省略字段 - 写之前必须先读全局变量，确认数据完整再写 - 输出长度控制在3000字以内 
```

| **输出** | 完整 System Prompt（如上） |
| -------- | -------------------------- |
|          |                            |

> [是] `@三读法解析工具` → 对应 Step 3 工具①
> [是] `{bid_document}` `{company_info}` `{parse_result}` → 对应 Step 5 记忆变量

------

## Step 2｜配置模型设置

| 项目     | 说明                      |
| -------- | ------------------------- |
| **目的** | 定义 Agent 的推理引擎参数 |
| **操作** | 按下表逐项填写            |

| 参数         | 定义                                                   | 推荐值       | 说明                                                         |
| ------------ | ------------------------------------------------------ | ------------ | ------------------------------------------------------------ |
| 规划思考模型 | 负责任务拆解与推理（如拆解招标文件结构、规划解析步骤） | 独立设置参数 | 与总结模型分开配置                                           |
| 总结生成模型 | 负责输出最终结构化 JSON 结果                           | 独立设置参数 | 与规划模型分开配置                                           |
| 对话轮数     | 传入大模型上下文的最大对话轮数                         | **2**        | 值越大，多轮上下文相关性越强，但 Tokens 消耗越多             |
| 最大思考次数 | 一轮对话中组件调用的最大次数                           | **2**        | 值越大，可处理更复杂任务，但响应耗时↑、Tokens↑；值越小，可减少组件重复调用 |

| **输出** | 规划模型参数 + 总结模型参数 + 对话轮数 + 最大思考次数 |
| -------- | ----------------------------------------------------- |
|          |                                                       |

| 子项         | 参数         | 值   | 说明                                                         |
| ------------ | ------------ | ---- | ------------------------------------------------------------ |
| 规划思考模型 | temperature  | 0.3  | 低温度，保证解析步骤不发散                                   |
|              | top_p        | 0.9  |                                                              |
|              | max_tokens   | 2048 | 单步推理，不需要长文本                                       |
|              | 深度思考     | 是   | 解析专家调用4个自定义组件，需思考过程保证解析准确率                                                              |
| 总结生成模型 | temperature  | 0.1  | 极低温度，JSON 输出必须精确，不允许创意                      |
|              | top_p        | 0.9  |                                                              |
|              | max_tokens   | 4096 | JSON 可能较长，预留足够空间                                  |
|              | 深度思考     | 是   | 解析专家调用4个自定义组件，需思考过程保证解析准确率                                                              |
|              | 检索增强     | 是   | 开启后，会根据用户query动态判断是否触发搜索，以提供更精准的答案。如果触发检索会产生额外的资源消耗， |
| 全局         | 对话轮数     | 2    | 多轮场景少，单次上传即解析                                   |
|              | 最大思考次数 | 2    | 配合 4 个工具，2 次调用足够完成一轮解析                      |

------

## Step 3｜配置工具

| 项目     | 说明                                                         |
| -------- | ------------------------------------------------------------ |
| **目的** | 扩展 Agent 的执行能力                                        |
| **操作** | 添加函数 / API / 插件，在角色指令中通过 `@工具名` 调用       |
| **约束** | **建议不超过 4 个**                                          |
| **原因** | 配合「最大思考次数 = 2」，超过 4 个会导致单轮内无法充分调用，降低准确率，增加无效调用和重复调用 |

> 以下为基于最佳实践的参考定义，工程落地时需根据实际接口和能力进行匹配调整。

| 序号 | 工具名称        | 类型     | 描述                                                         |
| ---- | --------------- | -------- | ------------------------------------------------------------ |
| 1    | @文件读取工具   | function | 读取全局变量中的 {bid_document}（招标文件内容）和 {company_info}（公司信息），返回结构化文本 |
| 2    | @三读法解析工具 | function | 调用 @三读法SOP 知识库，按「一读通览→二读精析→三读校验」流程逐层解析，输出中间结构化数据 |
| 3    | @JSON生成工具   | function | 将解析结果按角色指令要求的字段格式，生成标准 JSON，写入 {parse_result} |
| 4    | @风险标记工具   | function | 扫描解析结果，对照资质要求、时间节点、业绩条件，自动标记风险项，填充 risk_flags 数组 |

| **输出** | 工具列表（4 个），含名称、类型、描述 |
| -------- | ------------------------------------ |
|          |                                      |

> [工具] 工程落地调整建议：
>
> - 如果招标文件是 PDF/图片，工具①需接入 OCR 能力
> - 如果风险规则复杂，工具④可拆为多个子工具，但总数仍控制在 4 个以内
> - 工具②是核心，必须与知识库「三读法SOP」强绑定

------

## Step 4｜配置知识

| 项目     | 说明                                                |
| -------- | --------------------------------------------------- |
| **目的** | 构建 Agent 的知识来源，用户发消息时可引用知识库答复 |
| **操作** | 上传或关联知识库，按下表填写                        |

| 配置项 | 说明 | 约束 |
|--------|------|
| 知识库类型 | ① 本地文本数据 ② 表格型数据（含 FAQ 问答、多列索引问答）③ 在线网页数据 | — |
| 关联数量 | **最多 50 个** | 超过 50 个配合「最大思考次数 = 2」无法有效检索 |
| 备注信息 | **建议详细填写** | 写清知识类型和模式，提高问答匹配准确率 |

| 序号 | 知识库名称         | 类型        | 来源（路径/URL）                  | 备注                                                         |
| ---- | ------------------ | ----------- | --------------------------------- | ------------------------------------------------------------ |
| 1    | 三读法SOP          | local_text  | /knowledge/three_read_sop.md      | 核心知识库：一读通览→二读精析→三读校验的完整流程和字段映射规则 |
| 2    | 招投标法律法规汇编 | local_text  | /knowledge/law_bidding.txt        | 包含政府采购法、招标投标法及实施条例，用于风险标记参考       |
| 3    | 资质要求FAQ        | table_faq   | /knowledge/qualification_faq.xlsx | FAQ 模式：一问一答，如"工程类项目需要哪些资质"               |
| 4    | 评分标准索引表     | table_index | /knowledge/scoring_index.xlsx     | 多列索引模式：按项目类型、评分方法、权重筛选                 |
| 5    | 投标文件模板库     | local_text  | /knowledge/templates/             | 含工程类、货物类、服务类模板，用于 mandatory_documents 匹配  |
| 6    | 废标条件清单       | table_faq   | /knowledge/invalid_bid_faq.xlsx   | FAQ 模式，用于 risk_flags 填充                               |
| 7    | 行业术语词库       | local_text  | /knowledge/terms_bidding.txt      | 招投标专用术语及释义，提升解析准确率                         |
| 8    | 各省招投标平台规则 | web_page    | https://...                       | 备注：按省份分类，检索时优先匹配用户所在省                   |
| 9~50 | （按需扩展）       |             |                                   | 每项必须填写备注                                             |

| **输出** | 知识库列表（≤ 50 个），含名称、类型、来源、备注 |
|----------|

> [注意] 知识库①「三读法SOP」是工具②的直接依赖，必须优先配置，备注中需写清三读法的字段映射规则，否则工具②无法正确输出中间数据。

------

## Step 5｜配置记忆

| 项目     | 说明                                        |
| -------- | ------------------------------------------- |
| **目的** | 让 Agent 记住对话中的用户信息，回答更个性化 |
| **操作** | 按三种类型分别配置                          |

### 5.1 记忆变量

| 项目     | 说明                                     |
| -------- | ---------------------------------------- |
| 定义     | 记录**一维、单个**的应用信息或用户信息   |
| 结构     | Key-Value                                |
| 适用场景 | 让回答更个性化（如当前项目名、截止日期） |
| 关联     | 直接被角色指令中的 `{变量名}` 引用       |

| Key | Value（初始为空，待运行时填充） | 描述 |
|-----|-------------------------------|
| bid_document | {待运行时填充} | 招标文件完整文本内容 |
| company_info | {待运行时填充} | 投标公司信息（名称、资质、业绩等） |
| parse_result | {待运行时填充} | 解析输出的 JSON 结果 |
| current_project_name | {待运行时填充} | 当前项目名称，从 bid_document 中提取 |
| bid_deadline | {待运行时填充} | 投标截止时间，从 bid_document 中提取 |

### 5.2 记忆表

| 项目     | 说明                                     |
| -------- | ---------------------------------------- |
| 定义     | 记录**多维、大量**的应用信息或用户信息   |
| 结构     | 结构化表格，支持**智能问数**             |
| 适用场景 | 复杂数据查询（如历史项目列表、资质清单） |

| 表名         | 列名                                     | 数据（初始为空） | 描述                                                   |
| ------------ | ---------------------------------------- | ---------------- | ------------------------------------------------------ |
| 解析历史记录 | [项目名称, 解析日期, 风险项数量, 匹配度] | [待对话中积累]   | 记录每次解析的关键指标，支持问数如"哪个项目风险项最多" |
| 资质清单     | [资质名称, 等级, 有效期, 状态]           | [待对话中积累]   | 记录已识别的资质信息，支持问数如"哪些资质快过期了"     |

### 5.3 记忆片段

| 项目     | 说明                                                   |
| -------- | ------------------------------------------------------ |
| 定义     | 记录所有关于**用户信息、用户偏好、用户计划**的文本片段 |
| 结构     | 非结构化，自由文本                                     |
| 适用场景 | 长周期上下文召回                                       |

| 内容（初始为空） | 标签 | 描述 |
|------------------|------|
| {待对话中积累} | preference | 用户偏好的解析方式，如"优先看废标条件" |
| {待对话中积累} | plan | 用户的投标计划，如"下周三前要完成解析" |
| {待对话中积累} | user_info | 用户背景信息，如"甲方采购负责人" |

| **输出** | 记忆变量（5个）+ 记忆表（2个）+ 记忆片段（3个），三类分别填写 |
| -------- | ------------------------------------------------------------ |
|          |                                                              |

------

## Step 6｜配置对话

| 项目     | 说明                                           |
| -------- | ---------------------------------------------- |
| **目的** | 配置交互层，不影响核心逻辑，但直接影响用户体验 |
| **操作** | 按下表逐项填写                                 |

| 配置项   | 说明                           | 填入内容                                                     |
| -------- | ------------------------------ | ------------------------------------------------------------ |
| 声音     | 语音合成，填写 voice_id        | `bidding_expert_male`（专业沉稳男声）                        |
| 形象     | 数字人形象，填写 avatar_url    | `/avatar/bidding_expert.png`（商务正装形象）                 |
| 开场白   | 首次进入时的主动引导语         | "您好，我是招投标文件解析专家。请上传招标文件和公司信息，我将按三读法为您输出结构化解析结果。" |
| 推荐问题 | 引导用户快速发起对话的预设问题 | ① "帮我解析这份招标文件的关键信息" ② "检查投标文件是否满足资质要求" ③ "输出这份招标文件的JSON结构化数据" |
| 追问     | 对话中的智能追问策略           | `auto`（自动判断是否需要追问，如 bid_document 不完整时主动询问） |
| 背景图片 | 对话界面背景视觉               | `/bg/office_blue.jpg`（深蓝色商务风格）                      |

| **输出** | 对话配置完整填写 |
| -------- | ---------------- |
|          |                  |

------

## 依赖关系总览

```
Step 1 角色指令  ├─ @三读法解析工具 ← Step 3 工具②  ├─ @三读法SOP知识库 ← Step 4 知识库①  └─ {bid_document} {company_info} {parse_result} ← Step 5 记忆变量       ↓ Step 2 模型设置（全局控制）  ├─ 对话轮数 = 2  └─ 最大思考次数 = 2       ↓ Step 3 工具（4个）  ├─ ① 文件读取工具 → 读取 {bid_document} {company_info}  ├─ ② 三读法解析工具 → 调用 @三读法SOP知识库  ├─ ③ JSON生成工具 → 写入 {parse_result}  └─ ④ 风险标记工具 → 填充 risk_flags       ↓ Step 4 知识（≤50个）  └─ ① 三读法SOP 是工具②的直接依赖       ↓ Step 5 记忆  └─ 为 Step 1 提供 {变量名} 引用源       ↓ Step 6 对话（独立层，不影响逻辑） 
```

------

## 配置完成检查清单

| 检查项                                                       | 状态 |
| ------------------------------------------------------------ | ---- |
| 角色指令中 `@三读法解析工具` 和 `{bid_document}` `{company_info}` `{parse_result}` 引用是否与实际配置一致 | ☐    |
| 规划模型（temp=0.3）和总结模型（temp=0.1）参数是否独立设置   | ☐    |
| 对话轮数 = 2，最大思考次数 = 2                               | ☐    |
| 工具数量 = 4 个（未超过上限）                                | ☐    |
| 知识库数量 ≤ 50 个，且每项备注已填写，知识库①备注含三读法字段映射规则 | ☐    |
| 记忆变量 / 记忆表 / 记忆片段是否按场景区分                   | ☐    |
| 对话层 6 项是否全部配置                                      | ☐    |

------

# 第四章：标书撰写专家 Agent（子Agent 2）配置操作手册（SOP）

------

## 概述

> **配置总览顺序：角色指令 → 模型设置 → 深度思考 → 工具 → 知识 → 记忆 → 对话**

------

## Step 1｜配置角色指令

| 项目         | 说明                                        |
| ------------ | ------------------------------------------- |
| **目的**     | 定义 Agent 的行为宪法                       |
| **语法支持** | `@工具名` 调用工具，`{变量名}` 引用记忆变量 |
| **输出**     | 完整的 System Prompt + User Prompt          |

### System Prompt

```
# Role: 标书撰写专家 # Profile: 你是一个精确、规范的招投标文件撰写专家。你的唯一职责是根据解析结果和公司数据，按照@标书生成工具 调用 @标书模板知识库 生成投标文件草稿。你不做任何判断、不做任何决策、不生成任何最终文件。 # 核心任务： 1. 读取全局变量中的 {parsed_data} 和 {company_data} 2. 调用 @标书生成工具，结合 @标书模板知识库 生成投标文件草稿 3. 输出结构化投标文件内容，写入 {draft_document} # 输出必须包含以下模块： - 技术方案（按模板结构） - 商务报价（按模板结构） - 资质证明清单（按模板结构） - 业绩证明清单（按模板结构） # 约束规则（必须严格遵守）： - 你只做撰写，不做任何判断 - 你只输出投标文件草稿，不输出任何解释性文字 - 所有数据必须从 {parsed_data} 和 {company_data} 中提取，不得虚构 - 如果某模块在数据中不存在，填"待补充"，不要省略模块 - 写之前必须先读全局变量，确认数据完整再写 - 输出长度控制在5000字以内 
```

### User Prompt

```
请作为标书撰写专家，根据以下数据生成投标文件草稿： 解析结果：{{parsed_data}} 公司数据：{{company_data}} 请按模板结构生成完整草稿。 
```

| **输出** | System Prompt + User Prompt（如上） |
| -------- | ----------------------------------- |
|          |                                     |

> [是] `{parsed_data}` `{company_data}` `{draft_document}` → Step 5 记忆变量

------

## Step 2｜配置模型设置

| 子项         | 参数         | 值   | 说明                                                         |
| ------------ | ------------ | ---- | ------------------------------------------------------------ |
| 规划思考模型 | temperature  | 0.3  | 低温度，保证撰写结构不发散，严格按模板来                     |
|              | top_p        | 0.9  | 通用推荐值，平衡准确与自然                                   |
|              | max_tokens   | 2048 | 单步推理，规划撰写结构不需要长文本                           |
|              | 深度思考     | 是   | 开启后可输出思考过程，配合4个自定义组件保证草稿质量          |
| 总结生成模型 | temperature  | 0.3  | 中低温度，文本需流畅但必须严格遵循模板，不允许创意偏差       |
|              | top_p        | 0.9  | 通用推荐值                                                   |
|              | max_tokens   | 4096 | 投标文件较长，预留足够空间                                   |
|              | 深度思考     | 是   | 开启后可输出思考过程，保证各模块数据提取准确                 |
|              | 检索增强     | 是   | 开启后根据query动态触发知识库检索，提升模板匹配和数据提取精准度，触发检索会产生额外资源消耗 |
| 全局         | 对话轮数     | 2    | 撰写任务单次完成，多轮场景少                                 |
|              | 最大思考次数 | 2    | 配合4个工具，2次调用足够完成一轮撰写                         |

| **输出** | 模型配置完整填写（如上表） |
|----------|

------

## Step 2.5｜配置深度思考 [重点] 独立设置

| 配置项     | 说明                                                   |
| ---------- | ------------------------------------------------------ |
| **关闭 [否]** | 隐藏思考过程、更快响应，建议搭配官方组件               |
| **开启 [是]** | 可输出思考过程，适合与多个自定义组件配合，提升输出质量 |

| 本Agent配置 | 值 | 理由 |
|-------------|-----|
| **深度思考** | **开启 [是]** | 撰写专家调用4个自定义组件（数据库查询/标书生成/合规预检/格式校验），需思考过程保证草稿质量 |

| **输出** | 深度思考 = 开启 [是] |
|----------|

------

## Step 3｜配置工具

| 序号 | 工具名称        | 类型     | 描述                                                         |
| ---- | --------------- | -------- | ------------------------------------------------------------ |
| 1    | @数据库查询工具 | function | 从结构化数据库查询公司资质/业绩/财务数据，填充 {company_data} |
| 2    | @标书生成工具   | function | 读取 {parsed_data} 和 {company_data}，调用 @标书模板知识库，按模板结构生成草稿，写入 {draft_document} |
| 3    | @合规预检工具   | function | 对照《红线清单》知识库，检查草稿中是否存在废标风险项，输出风险标记 |
| 4    | @格式校验工具   | function | 检查输出是否符合标书模板要求（章节完整性、必填项），不合规则返回修正提示 |

| **输出** | 工具列表（4 个） |
| -------- | ---------------- |
|          |                  |

------

## Step 4｜配置知识

| 序号 | 知识库名称           | 类型        | 来源                                | 备注                                                         |
| ---- | -------------------- | ----------- | ----------------------------------- | ------------------------------------------------------------ |
| 1    | 标书模板知识库       | local_text  | /knowledge/bid_templates/           | 核心：含技术方案/商务报价/资质清单/业绩清单模板，注明适用项目类型（工程/货物/服务） |
| 2    | 招投标法律法规汇编   | local_text  | /knowledge/law_bidding.txt          | 合规预检参考                                                 |
| 3    | 红线清单（废标条件） | table_faq   | /knowledge/redline_faq.xlsx         | FAQ模式，工具③直接依赖                                       |
| 4    | 资质要求索引表       | table_index | /knowledge/qualification_index.xlsx | 多列索引模式，工具①查询依赖                                  |
| 5    | 行业术语词库         | local_text  | /knowledge/terms_bidding.txt        | 提升撰写用词准确性                                           |
| 6    | 评分标准知识库       | local_text  | /knowledge/scoring_criteria.txt     | 撰写时参考评分权重，优先突出高分项                           |
| 7    | 优秀标书案例库       | local_text  | /knowledge/examples/                | 按行业分类，供撰写时参考结构和措辞                           |
| 8    | 各省招投标平台规则   | web_page    | https://...                         | 按省份分类，检索时优先匹配用户所在省                         |
| 9~50 | （按需扩展）         |             |                                     | 每项必须填写备注                                             |

| **输出** | 知识库列表（≤ 50 个） |
|----------|

------

## Step 5｜配置记忆

### 5.1 记忆变量

| Key                  | 描述                                     |
| -------------------- | ---------------------------------------- |
| parsed_data          | 解析专家输出的JSON结构化数据             |
| company_data         | 从数据库查询到的公司资质/业绩数据        |
| draft_document       | 撰写完成的投标文件草稿                   |
| current_project_name | 当前项目名称，从 parsed_data 中提取      |
| bid_type             | 项目类型（工程/货物/服务），用于匹配模板 |

### 5.2 记忆表

| 表名         | 列名                                     | 描述                                       |
| ------------ | ---------------------------------------- | ------------------------------------------ |
| 撰写历史记录 | [项目名称, 撰写日期, 模块数, 自检通过率] | 支持问数，如"哪个项目自检通过率最低"       |
| 公司资质清单 | [资质名称, 等级, 有效期, 状态]           | 从数据库同步，支持问数如"哪些资质快过期了" |

### 5.3 记忆片段

| 内容     | 标签       | 描述                                   |
| -------- | ---------- | -------------------------------------- |
| {待积累} | preference | 用户偏好的撰写风格，如"技术方案要详细" |
| {待积累} | plan       | 用户的投标计划，如"周五前要提交草稿"   |
| {待积累} | user_info  | 用户背景信息，如"投标负责人"           |

| **输出** | 记忆变量（5个）+ 记忆表（2个）+ 记忆片段（3个） |
| -------- | ----------------------------------------------- |
|          |                                                 |

------

## Step 6｜配置对话

| 配置项   | 填入内容                                                     |
| -------- | ------------------------------------------------------------ |
| 声音     | `bidding_writer_male`                                        |
| 形象     | `/avatar/bidding_writer.png`                                 |
| 开场白   | 您好，我是标书撰写专家。解析数据已就绪，请确认项目信息后，我将为您生成投标文件草稿。 |
| 推荐问题 | ① "生成技术方案草稿" ② "生成商务报价模块" ③ "输出完整投标文件草稿" |
| 追问     | `auto`                                                       |
| 背景图片 | `/bg/office_green.jpg`                                       |

| **输出** | 对话配置完整填写 |
| -------- | ---------------- |
|          |                  |

------

## 依赖关系总览

```
Step 1 角色指令  ├─ @标书生成工具 ← Step 3 工具②  ├─ @标书模板知识库 ← Step 4 知识库①  └─ {parsed_data} {company_data} {draft_document} ← Step 5 记忆变量       ↓ Step 2 模型设置  ├─ 对话轮数 = 2  ├─ 最大思考次数 = 2  └─ 深度思考 = 开启 [是] ← 撰写专家调用多个自定义组件，建议开启       ↓ Step 2.5 深度思考（独立配置）  └─ 开启 [是]       ↓ Step 3 工具（4个）  ├─ ① 数据库查询 → 填充 {company_data}  ├─ ② 标书生成 → 调用知识库，写入 {draft_document}  ├─ ③ 合规预检 → 对照红线清单  └─ ④ 格式校验 → 检查模板合规性       ↓ Step 4 知识（≤50个）  └─ ① 标书模板知识库 → 工具②直接依赖  └─ ③ 红线清单 → 工具③直接依赖       ↓ Step 5 记忆 → 为 Step 1 提供 {变量名} 引用源       ↓ Step 6 对话（独立层） 
```

------

## 已记录的配置选择（子Agent 2）

| 配置项               | 你的选择  |
| -------------------- | --------- |
| 规划模型 temperature | 0.3       |
| 总结模型 temperature | 0.3       |
| top_p                | 0.9       |
| 总结模型 max_tokens  | 4096      |
| 深度思考             | 开启 [是]    |
| 检索增强             | 开启 [是]    |
| 对话轮数             | 2         |
| 最大思考次数         | 2         |
| 工具数量             | 4 个      |
| 知识库数量           | ≤50 个    |
| 开场白               | 已提供    |
| 推荐问题             | 已提供3条 |
| System Prompt        | 已提供    |
| User Prompt          | 已提供    |

------

------

# 第五章：合规与质量审核专家 Agent（子Agent 3）配置操作手册（SOP）

------

## 概述

> **配置总览顺序：角色指令 → 模型设置 → 深度思考 → 工具 → 知识 → 记忆 → 对话**

------

## Step 1｜配置角色指令

| 项目         | 说明                                        |
| ------------ | ------------------------------------------- |
| **目的**     | 定义 Agent 的行为宪法                       |
| **语法支持** | `@工具名` 调用工具，`{变量名}` 引用记忆变量 |
| **输出**     | 完整的 System Prompt + User Prompt          |

### System Prompt

```
# Role: 合规与质量审核专家 # Profile: 你是一个严谨、细致的招投标文件合规与质量审核专家。你的唯一职责是读取全局变量中的投标文件草稿，对照《红线清单》和评分标准进行逐项质检，输出审核结果。你不做任何修改、不做任何决策、不生成任何文件。 # 核心任务： 1. 读取全局变量中的 {draft_document} 和 {parsed_data} 2. 调用 @红线对照工具，逐项检查是否存在废标风险 3. 调用 @评分模拟工具，对照评分标准检查得分项完整性 4. 调用 @格式校验工具，检查是否符合标书模板要求 5. 输出结构化审核结果，写入 {review_result} # 审核必须覆盖以下维度： - 废标项检查（红线清单）：资质不符/业绩缺失/签字盖章缺失/投标有效期不足等 - 评分项检查（评分标准）：技术方案/商务报价/资质业绩/项目团队等得分项是否完整 - 格式合规检查（标书模板）：章节完整性/必填项/排版规范 # 输出必须包含以下字段： - review_status: 通过 / 不通过 / 有条件通过 - risk_flags: 风险项列表（如"资质不足"/"缺少签字页"） - score_gaps: 缺失得分项列表 - format_issues: 格式问题列表 - retry_suggestions: 修改建议（仅在不通过时填写） # 约束规则（必须严格遵守）： - 你只做审核，不做任何修改 - 你只输出审核结果，不输出任何解释性文字 - 所有判断必须基于 {parsed_data} 和 {draft_document} 中的内容，不得主观臆断 - 风险项必须引用原文位置，格式为"第X章第X节：具体问题" - 写之前必须先读全局变量，确认数据完整再写 - 输出长度控制在3000字以内 - 审核不通过时，重试次数+1，最多允许3次重试 
```

### User Prompt

```
请作为合规与质量审核专家，审核以下投标文件草稿： 草稿内容：{{draft_document}} 解析结果：{{parsed_data}} 请按审核规则执行质检。 
```

| **输出** | System Prompt + User Prompt（如上） |
| -------- | ----------------------------------- |
|          |                                     |

> [是] `{draft_document}` `{parsed_data}` `{review_result}` → Step 5 记忆变量

------

## Step 2｜配置模型设置

| 子项         | 参数         | 值   | 说明                                                         |
| ------------ | ------------ | ---- | ------------------------------------------------------------ |
| 规划思考模型 | temperature  | 0.2  | 极低温度，审核判断必须绝对精确，不允许任何发散               |
|              | top_p        | 0.9  | 通用推荐值                                                   |
|              | max_tokens   | 2048 | 审核逻辑清晰，单步推理不需要长文本                           |
|              | 深度思考     | 是   | 开启后可输出思考过程，配合4个自定义组件保证审核精准度        |
| 总结生成模型 | temperature  | 0.1  | 极低温度，审核报告必须精确，不允许任何创意偏差               |
|              | top_p        | 0.9  | 通用推荐值                                                   |
|              | max_tokens   | 4096 | 审核报告可能较长，需包含多个维度的检查结果，预留足够空间     |
|              | 深度思考     | 是   | 开启后可输出思考过程，保证每个风险项都有依据                 |
|              | 检索增强     | 是   | 开启后根据query动态触发知识库检索，提升红线清单和评分标准的匹配精准度，触发检索会产生额外资源消耗 |
| 全局         | 对话轮数     | 2    | 审核任务单次完成，多轮场景少                                 |
|              | 最大思考次数 | 2    | 配合4个工具，2次调用足够完成一轮审核                         |

| **输出** | 模型配置完整填写（如上表） |
|----------|

------

## Step 2.5｜配置深度思考 [重点] 独立设置

| 配置项     | 说明                                                   |
| ---------- | ------------------------------------------------------ |
| **关闭 [否]** | 隐藏思考过程、更快响应，建议搭配官方组件               |
| **开启 [是]** | 可输出思考过程，适合与多个自定义组件配合，提升输出质量 |

| 本Agent配置 | 值 | 理由 |
|-------------|-----|
| **深度思考** | **开启 [是]** | 审核专家调用4个自定义组件（草稿读取/红线对照/评分模拟/格式校验），需要思考过程保证每个风险项都有原文依据 |

| **输出** | 深度思考 = 开启 [是] |
|----------|

------

## Step 3｜配置工具

| 序号 | 工具名称          | 类型     | 描述                                                         |
| ---- | ----------------- | -------- | ------------------------------------------------------------ |
| 1    | @草稿读取工具     | function | 从全局变量读取 {draft_document}，返回结构化文本              |
| 2    | @红线对照工具     | function | 调用 @红线清单知识库，逐条检查废标风险项，输出 risk_flags    |
| 3    | @评分模拟工具     | function | 调用 @评分标准知识库，对照评分项检查得分项完整性，输出 score_gaps |
| 4    | @审核报告生成工具 | function | 汇总风险项、缺失项、格式问题，生成结构化审核报告，写入 {review_result} |

| **输出** | 工具列表（4 个） |
| -------- | ---------------- |
|          |                  |

------

## Step 4｜配置知识

| 序号 | 知识库名称           | 类型       | 来源                            | 备注                                       |
| ---- | -------------------- | ---------- | ------------------------------- | ------------------------------------------ |
| 1    | 红线清单（废标条件） | table_faq  | /knowledge/redline_faq.xlsx     | 核心：FAQ模式，工具②直接依赖，必须逐条对照 |
| 2    | 评分标准知识库       | local_text | /knowledge/scoring_criteria.txt | 工具③直接依赖，检查得分项完整性            |
| 3    | 标书模板知识库       | local_text | /knowledge/bid_templates/       | 工具④直接依赖，检查格式合规性              |
| 4    | 招投标法律法规汇编   | local_text | /knowledge/law_bidding.txt      | 审核判断的法律依据                         |
| 5    | 行业术语词库         | local_text | /knowledge/terms_bidding.txt    | 提升审核用词准确性                         |
| 6    | 优秀标书案例库       | local_text | /knowledge/examples/            | 供审核时参考满分标准                       |
| 7    | 各省招投标平台规则   | web_page   | https://...                     | 按省份分类，检索时优先匹配用户所在省       |
| 8~50 | （按需扩展）         |            |                                 | 每项必须填写备注                           |

| **输出** | 知识库列表（≤ 50 个） |
|----------|

------

## Step 5｜配置记忆

### 5.1 记忆变量

| Key                  | 描述                         |
| -------------------- | ---------------------------- |
| draft_document       | 撰写专家输出的投标文件草稿   |
| parsed_data          | 解析专家输出的JSON结构化数据 |
| review_result        | 审核完成的结构化审核结果     |
| current_project_name | 当前项目名称                 |
| retry_count          | 当前重试次数（0~3）          |

### 5.2 记忆表

| 表名         | 列名                                                 | 描述                           |
| ------------ | ---------------------------------------------------- | ------------------------------ |
| 审核历史记录 | [项目名称, 审核日期, 审核结果, 风险项数量, 重试次数] | 支持问数                       |
| 风险项统计   | [风险类型, 出现次数, 涉及项目]                       | 支持问数，如"哪类风险出现最多" |

### 5.3 记忆片段

| 标签       | 描述                   |
| ---------- | ---------------------- |
| preference | 用户偏好的审核严格程度 |
| plan       | 项目投标计划           |
| user_info  | 用户背景信息           |

| **输出** | 记忆变量（5个）+ 记忆表（2个）+ 记忆片段（3个） |
|----------|

------

## Step 6｜配置对话

| 配置项   | 填入内容                                                     |
| -------- | ------------------------------------------------------------ |
| 声音     | `reviewer_male`                                              |
| 形象     | `/avatar/reviewer.png`                                       |
| 开场白   | 您好，我是合规与质量审核专家。草稿已接收，我将对照红线清单和评分标准进行逐项质检，请稍候。 |
| 推荐问题 | ① "审核这份投标文件草稿" ② "查看审核结果和风险项" ③ "审核不通过，帮我重新生成" |
| 追问     | `auto`                                                       |
| 背景图片 | `/bg/office_red.jpg`                                         |

| **输出** | 对话配置完整填写 |
|----------|

------

## 依赖关系总览

```
Step 1 角色指令  ├─ @红线对照工具 ← Step 3 工具②  ├─ @评分模拟工具 ← Step 3 工具③  ├─ @审核报告生成工具 ← Step 3 工具④  ├─ @红线清单知识库 ← Step 4 知识库①  ├─ @评分标准知识库 ← Step 4 知识库②  ├─ @标书模板知识库 ← Step 4 知识库③  └─ {draft_document} {parsed_data} {review_result} ← Step 5 记忆变量       ↓ Step 2 模型设置  ├─ 对话轮数 = 2  ├─ 最大思考次数 = 2  └─ 深度思考 = 开启 [是] ← 审核专家调用多个自定义组件，建议开启       ↓ Step 2.5 深度思考（独立配置）  └─ 开启 [是]       ↓ Step 3 工具（4个）  ├─ ① 草稿读取 → 读取变量  ├─ ② 红线对照 → 调用知识库，输出风险  ├─ ③ 评分模拟 → 调用知识库，输出缺失项  └─ ④ 审核报告 → 汇总写入结果       ↓ Step 4 知识（≤50个）  └─ ① 红线清单 → 工具②直接依赖  └─ ② 评分标准 → 工具③直接依赖  └─ ③ 标书模板 → 工具④直接依赖       ↓ Step 5 记忆 → 为 Step 1 提供 {变量名} 引用源       ↓ Step 6 对话（独立层） 
```

------

## 已记录的配置选择（子Agent 3）

| 配置项               | 你的选择                  |
| -------------------- | ------------------------- |
| 规划模型 temperature | 0.2（极低，审核必须精确） |
| 总结模型 temperature | 0.1（极低，报告必须精确） |
| top_p                | 0.9                       |
| 总结模型 max_tokens  | 4096                      |
| 深度思考             | 开启 [是]                    |
| 检索增强             | 开启 [是]                    |
| 对话轮数             | 2                         |
| 最大思考次数         | 2                         |
| 工具数量             | 4 个                      |
| 知识库数量           | ≤50 个                    |
| 开场白               | 已提供                    |
| 推荐问题             | 已提供3条                 |
| System Prompt        | 已提供                    |
| User Prompt          | 已提供                    |

------

#### 第六章：全局工作流（Workflow）串联实操

- **6.1 主流程节点连线图**：从 Start 到 End 的完整连线步骤。
- **6.2 关键条件分支设置**：如何设置“审核不通过返回重写”的循环条件。
- **6.3 异常处理与兜底**：大文件超限、API 调用失败的容错配置。

------

## 三个Agent配置对比总表（更新版）

| 配置项              | 主Agent（项目总监） | 子Agent 1（解析专家）          | 子Agent 2（撰写专家）            | 子Agent 3（审核专家）              |
| ------------------- | ------------------- | ------------------------------ | -------------------------------- | ---------------------------------- |
| 规划模型 temp       | 0.2                 | 0.3                            | 0.3                              | 0.2                                |
| 总结模型 temp       | 0.1                 | 0.1                            | 0.3                              | 0.1                                |
| top_p               | 0.9                 | 0.9                            | 0.9                              | 0.9                                |
| 总结模型 max_tokens | 2048                | 4096                           | 4096                             | 4096                               |
| **深度思考**        | **开启 [是]**          | **开启 [是]**                     | **开启 [是]**                       | **开启 [是]**                         |
| 检索增强            | 是                  | 是                             | 是                               | 是                                 |
| 对话轮数            | 2                   | 2                              | 2                                | 2                                  |
| 最大思考次数        | 2                   | 2                              | 2                                | 2                                  |
| 工具数量            | 4                   | 4                              | 4                                | 4                                  |
| 理由                | 官方组件快速响应    | 自定义组件多，需思考保证准确率 | 自定义组件多，需思考保证草稿质量 | 自定义组件多，需思考保证审核精准度 |

